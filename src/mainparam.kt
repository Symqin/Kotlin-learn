// main parameter example in Kotlin

fun main(args: Array<String>) {
    for (arg in args) {
        println(arg)
    }
}

// To run this program with arguments gunakan agrgumen di terminal atau di jetbrains nya